# Md Yasin Portfolio — Vercel Deployment & Admin Automation Guide

এই নির্দেশিকাটি অনুসরণ করে আপনি আপনার পোর্টফোলিও ওয়েবসাইটটি **Vercel**-এ সম্পূর্ণ অটোমেটেডভাবে ডেপ্লয় করতে পারবেন এবং **Admin Panel** থেকে আপনার কাজ (Projects / Apps), স্কিল (Skills) এবং নতুন আইডিয়া (Ideas Lab) কোডিং ছাড়াই সরাসরি ব্রাউজার থেকে লাইভ আপডেট করতে পারবেন।

---

## ১. অ্যাডমিন প্যানেলে প্রবেশ ও ব্যবহারের নিয়ম (Admin Panel Guide)

### কীভাবে অ্যাডমিন প্যানেল ওপেন করবেন?
1. **সরাসরি URL দিয়ে:** আপনার ব্রাউজারের অ্যাড্রেস বারে যান এবং ওয়েবসাইটের লিংকের শেষে `/admin` অথবা `/#admin` লিখুন। (যেমন: `https://your-domain.vercel.app/admin` বা `http://localhost:3000/#admin`)
2. **ফুটার থেকে:** ওয়েবসাইটের একদম নিচে (Footer) কপিরাইট সেকশনে একটি **`Admin`** বাটন দেওয়া আছে, সেখানে ক্লিক করলেই অ্যাডমিন লগইন বক্স ওপেন হবে।

### লগইন পিন (Default Security PIN):
- **ডিফল্ট মাস্টার পিন:** `7860`
- লগইন করার পর **Settings** ট্যাব থেকে আপনার পছন্দমতো যেকোনো ৪ বা তার বেশি ডিজিটের নতুন পিন সেট করতে পারবেন।

### অ্যাডমিন প্যানেলের প্রধান ফিচারসমূহ:
1. **কাজ ও অ্যাপস (Projects & Apps Manager):**
   - **নতুন কাজ সম্পন্ন হলে:** "নতুন কাজ/অ্যাপ যোগ করুন" বাটনে ক্লিক করে অ্যাপের নাম, ক্যাটাগরি, সাবটাইটেল, ইমেজ লিংক, লাইভ ডেমো লিংক, গিটহাব রিপো লিংক এবং ট্যাগস বসিয়ে সেভ করুন।
   - এটি সাথে সাথে ওয়েবসাইটের "Featured Portfolio Showcase" এবং "Showreel"-এ লাইভ হয়ে যাবে।
   - যেকোনো প্রজেক্ট এডিট করতে পারবেন বা পুরনো প্রজেক্ট ডিলিট করতে পারবেন।
   - "Featured" চেকমার্ক দিলে প্রজেক্টটি হোমপেজের প্রধান গ্রিডে হাইলাইট হবে।

2. **দক্ষতা ও টেক স্ট্যাক (Skills Manager):**
   - নতুন কোনো টেকনোলজি শিখলে বা সার্ভিস যুক্ত করতে চাইলে ক্যাটাগরি সিলেক্ট করে নতুন স্কিল যুক্ত করুন।
   - ৩ডি সোলার সিস্টেম (Interactive 3D Solar System)-এ রিয়েলটাইমে সেই স্কিলটির প্ল্যানেটারি অরবিট ঘুরতে থাকবে।

3. **নতুন আইডিয়া ও ল্যাব (Ideas & Innovation Lab):**
   - আপনার নতুন কোনো কনসেপ্ট, চলমান গবেষণা বা আসন্ন অ্যাপ থাকলে এখানে স্ট্যাটাস (Concept, In Development, Prototype, MVP Ready) দিয়ে এন্ট্রি করতে পারবেন।
   - ভিজিটররা আপনার ভবিষ্যৎ প্রোডাক্ট এবং প্রোটোটাইপ দেখতে পাবে।

4. **মেসেজ ও ক্লায়েন্ট বুকিং (Inbox):**
   - ওয়েবসাইট থেকে ভিজিটর ও ক্লায়েন্টদের পাঠানো মেসেজ এবং শিডিউল কল রিকোয়েস্ট সরাসরি এখানে জমা হবে।

5. **প্রোফাইল ও সেটিংস:**
   - আপনার বায়ো, কন্টাক্ট ইমেইল, সোশ্যাল মিডিয়া লিংক এবং অ্যাডমিন পিন এক ক্লিকে আপডেট করুন।

6. **Sync Defaults:**
   - কোনো কারণে ডেটা রিসেট বা ডাটাবেসে আবার ডিফল্ট ডেটা সিঙ্ক করতে চাইলে উপরের ডানপাশের "Sync Defaults" বাটনে ক্লিক করলেই সবকিছু ইনস্ট্যান্ট রিসেট হয়ে যাবে।

---

## ২. Vercel-এ অটোমেটেড ডেপ্লয় করার সহজ ধাপ (Step-by-Step Vercel Deployment)

প্রজেক্টটিতে ইতিমধ্যে `vercel.json` এবং সার্ভারলেস API কনফিগারেশন (`/api/send-booking` ও `/api/health`) তৈরি করে দেওয়া হয়েছে। গিটহাবে পুশ করলেই Vercel স্বয়ংক্রিয়ভাবে বিল্ড করে লাইভ করে দেবে।

### ধাপ ১: গিটহাবে কোড আপলোড করুন (GitHub Push)
টার্মিনালে এই কমান্ডগুলো রান করে আপনার GitHub রিপোজিটরিতে কোড পুশ করুন:

```bash
git add .
git commit -m "Add Dynamic Admin Panel and Vercel Automation"
git push origin main
```

### ধাপ ২: Vercel-এ লগইন ও রিপোজিটরি ইমপোর্ট করুন
1. [vercel.com](https://vercel.com)-এ যান এবং আপনার GitHub অ্যাকাউন্ট দিয়ে লগইন করুন।
2. **"Add New..."** -> **"Project"**-এ ক্লিক করুন।
3. আপনার `md-yasin-portfolio` রিপোজিটরির পাশে থাকা **"Import"** বাটনে ক্লিক করুন।

### ধাপ ৩: Environment Variables যোগ করুন (ঐচ্ছিক কিন্তু সাজেস্টেড)
Vercel এর Configure Project স্ক্রিনে:
- **Framework Preset:** `Vite` (স্বয়ংক্রিয়ভাবে সিলেক্ট হবে)
- **Root Directory:** `./`
- **Environment Variables** সেকশনে নিচের ভেরিয়েবলগুলো দিতে পারেন:
  - `SENDGRID_API_KEY`: আপনার সেন্ডগ্রিড এপিআই কি (ইমেইল নোটিফিকেশনের জন্য)
  - `NOTIFICATION_EMAIL`: `mdyasinarafath20@gmail.com`
  - `VERIFIED_SENDER_EMAIL`: আপনার সেন্ডগ্রিড ভেরিফাইড ইমেইল

### ধাপ ৪: "Deploy" বাটনে ক্লিক করুন
- **Deploy** বাটনে ক্লিক করলেই ১ মিনিটের মধ্যে আপনার ওয়েবসাইট বিশ্বব্যাপী CDN-এ লাইভ হয়ে যাবে।
- Vercel আপনাকে একটি ফ্রি ডোমেইন দেবে (যেমন: `md-yasin-portfolio.vercel.app`)।

---

## ৩. ফুল অটোমেশন (CI/CD) কীভাবে কাজ করবে?
- এরপর থেকে আপনি যখনই গিটহাবে নতুন কোনো কোড পুশ করবেন, Vercel নিজে থেকেই সাইটটি অটো-বিল্ড করে আপডেট করে দেবে।
- আর নতুন কোনো কাজ, স্কিল বা আইডিয়া আপলোডের জন্য কোডও এডিট করতে হবে না—সরাসরি আপনার ওয়েবসাইটের অ্যাডমিন প্যানেল (`/admin`) থেকে ফোন বা ল্যাপটপ দিয়ে ১ সেকেন্ডে আপডেট করে দিতে পারবেন!
